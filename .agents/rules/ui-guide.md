---
trigger: always_on
---

- Use Next.js 15 App Router with TypeScript strict mode
- Use Tailwind CSS for all styling, no CSS modules
- Use @tanstack/react-query for all API calls — no raw fetch in components
- API base URL: http://localhost:8000/api/v1
- Dark mode first, support light mode via Tailwind dark: prefix
- All pages must be responsive (mobile-friendly)
- Use shadcn/ui components where applicable
- File naming: kebab-case, co-locate components with their pages
