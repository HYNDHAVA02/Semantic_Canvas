Before implementing $ARGUMENTS, create a plan.

1. Read relevant source files and tests to understand current state
2. Check if existing MCP tools, repository methods, or REST endpoints already cover this
3. Propose a plan with:
   - Which files to create or modify (keep list short)
   - Database migration needed (if any)
   - MCP tool changes (if any)
   - REST endpoint changes (if any)
   - Test plan: what tests to write, what they verify
4. Keep each file change under 200 lines
5. Flag any architectural decisions that should be recorded in the decisions table

Do NOT write any code. Present the plan and wait for approval.
